{
  "name": "@remote-control/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "RemoteControl desktop client",
  "author": "RemoteControl",
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "node scripts/run-electron-vite.mjs dev",
    "dev:server-app": "node scripts/run-electron-vite.mjs dev host",
    "dev:client-app": "node scripts/run-electron-vite.mjs dev viewer",
    "preview": "node scripts/run-electron-vite.mjs preview",
    "build": "npm run typecheck && electron-vite build",
    "build:win": "npm run build && electron-builder --win portable",
    "prepare:backend": "node scripts/copy-backend.mjs",
    "build:server-app": "npm run build --workspace @remote-control/shared && npm run build --workspace @remote-control/server && npm run prepare:backend && cross-env REMOTE_CONTROL_APP_MODE=host npm run build && electron-builder --config electron-builder.server.yml --win portable",
    "build:client-app": "cross-env REMOTE_CONTROL_APP_MODE=viewer npm run build && electron-builder --config electron-builder.client.yml --win portable",
    "build:apps": "npm run build:server-app && npm run build:client-app",
    "typecheck": "tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit"
  },
  "build": {
    "appId": "dev.remote-control.desktop",
    "productName": "RemoteControl",
    "directories": {
      "output": "release"
    },
    "files": [
      "out/**/*",
      "package.json"
    ],
    "asar": true,
    "win": {
      "signAndEditExecutable": false,
      "target": [
        "portable"
      ]
    }
  },
  "dependencies": {
    "@remote-control/shared": "0.1.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@nestjs/platform-socket.io": "^11.0.0",
    "@nestjs/websockets": "^11.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "electron-vite": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "socket.io": "^4.8.0",
    "socket.io-client": "^4.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "cross-env": "^10.1.0",
    "electron": "39.8.9",
    "electron-builder": "^26.8.1",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "optionalDependencies": {
    "@nut-tree-fork/nut-js": "^4.2.6"
  }
}
